{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "AshipaOS x86_64 build target",
  "type": "object",
  "required": ["name", "arch", "status"],
  "properties": {
    "name": { "const": "x86_64" },
    "arch": { "const": "x86_64" },
    "status": { "type": "string", "enum": ["PROVISIONAL", "CONFIRMED"] }
  },
  "additionalProperties": true
}
