# amlogic-coreelec-fork

Pinned CoreELEC checkout location for the Amlogic track (guide Task 0.5.2).
The upstream CoreELEC source is checked out here at a pinned tag/commit and
is NOT source-controlled as part of AshipaOS; only the pin record
(repository/ref/commit) is recorded in the box configuration by a later
layer task.
