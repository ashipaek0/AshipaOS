name: pr
on:
  pull_request:
jobs:
  static-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run static tests
        run: |
          for t in tests/static/*.sh; do
            echo "=== $t ==="
            bash "$t"
          done
