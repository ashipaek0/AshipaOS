# A95X Storage Contract

The A95X image uses removable SD media with a DOS partition table, FAT16 boot partition labelled `A95XBOOT`, and ext4 root partition labelled `RootFS`. The pre-partition gap and boot file names are part of the validated contract. eMMC must never be overwritten.
